# 매일 시세 갱신 작업 지시서

이 저장소의 `index.html`은 https://ytw0805.github.io/babytrip/ 로 서비스되는
2026-10-03 ~ 10-11 후쿠오카·유후인 태교여행 플래너(2인)입니다.

이 페이지의 가치는 **가격이 실측이고, 출처가 있고, 조사일이 찍혀 있다**는 데
전적으로 의존합니다. 이전 버전이 추정치를 실제 가격처럼 보여줘 신뢰를 잃은 적이
있습니다. 절대 반복하지 마세요.

## 0. 날짜 확인

`date -u +%F` 를 실행해 오늘 날짜를 확인합니다. 절대 추측하지 마세요.

**2026-10-12 이후라면** 여행이 끝났습니다. 아무것도 바꾸지 말고, 커밋도 하지 말고,
"여행이 종료되어 갱신을 중단했다, https://claude.ai/code/routines 에서 루틴을 끌 수
있다"는 한 줄만 답하고 종료하세요.

## 1. 현재 값 읽기

`index.html`의 `<script>` 블록에서 `SURVEY_DATE`, `FLIGHT_MARKET`, `STAY_MARKET`을
찾아 현재 값을 확인합니다.

## 2. 시세 재측정 (WebFetch)

| 항목 | URL |
|---|---|
| 항공 A | https://www.kayak.co.kr/flight-routes/Incheon-Intl-ICN/Fukuoka-FUK |
| 항공 B | https://www.kayak.co.kr/%ED%95%AD%EA%B3%B5%EA%B6%8C/%EB%8C%80%ED%95%9C%EB%AF%BC%EA%B5%AD-KR0/%ED%9B%84%EC%BF%A0%EC%98%A4%EC%B9%B4%EA%B3%B5%ED%95%AD-FUK |
| 후쿠오카 | https://www.booking.com/city/jp/fukuoka-hakata.ko.html |
| 유후인 | https://www.booking.com/city/jp/yufuin.ko.html |

- **항공**: 1인 왕복 원화 기준 — 10월 평균, 9월 평균, 인천 출발 최저, 가장 싼 요일,
  가장 비싼 요일.
- **숙소**: 게재된 각 숙소의 이름 / 1박 원화 가격 / 평점 전부.
- 403·429·타임아웃이면 **1회만** 재시도하고, 그래도 안 되면 그 출처는 포기합니다.

## 3. 값 갱신

다음만 수정합니다.

- `SURVEY_DATE` → 오늘 날짜, `"2026년 9월 4일"` 형식
- `FLIGHT_MARKET` → `octAvg`, `sepAvg`, `icnLow`, `tueLow`, `friHigh`
- `STAY_MARKET.fukuoka` / `STAY_MARKET.yufuin` →
  - `low` 게재 최저가, `high` 게재 최고가, `mid` 게재가들의 평균
  - `floor` 페이지에 표시된 "최저 ○○원부터" 값
  - `list` 항목은 `["표시이름", 가격정수, 평점]`

### 절대 규칙 — 작업 완수보다 우선합니다

- **숫자를 지어내거나, 추정하거나, 보정하지 마세요.** 모든 수치는 가져온 페이지에
  문자 그대로 있어야 합니다.
- 출처 조회에 실패하면 **그 출처의 기존 값을 그대로 두고** 4단계에 실패를 명시합니다.
  최소 한 곳이라도 갱신됐을 때만 `SURVEY_DATE`를 올립니다. 갱신 안 됐는데 날짜만
  바꾸는 건 금지입니다.
- 그 외에는 아무것도 바꾸지 마세요 — 디자인, CSS, 레이아웃, 문구, 숙소 추천, 순위
  로직, `FLIGHT_LEG_BAND` 유도 방식 전부 그대로. **데이터 값만** 교체합니다.
- `index.html`은 `<!doctype>` / `<head>` / `charset` / `noindex`를 갖춘 독립 실행
  문서입니다. 이 구조를 절대 깨뜨리지 마세요.

## 4. 갱신 상태 표시

`renderBasis()` 안의 `basis-warn` 문구에, 오늘 어떤 출처가 갱신됐고 어떤 게
실패했는지 **한 줄**로 넣습니다. 예: `항공·후쿠오카 갱신 완료 · 유후인 조회 실패(이전 값 유지)`
나머지 경고 문구는 그대로 둡니다.

## 5. 커밋 & 푸시

`main`에 커밋하고 푸시합니다. GitHub Pages가 자동 배포합니다.
커밋 메시지 예: `Refresh market prices (2026-09-04)` — 본문에 어떤 출처가
갱신됐는지 적습니다.

**푸시 권한이 없으면** 브랜치를 만들어 PR을 열고, 답변에 PR 링크를 남기세요.

## 6. 보고

3~6줄로: 성공한 출처, 그리고 직전 대비 **10% 이상 움직인 수치**를 `이전 → 이후`로
적습니다. 유의미한 변동이 없으면 한 줄로 그렇게 씁니다.

## 2-b. WebFetch가 막혔을 때 (폴백)

클라우드 실행 환경의 이그레스 정책이 `kayak.co.kr` / `booking.com` 직접 접근을
차단할 수 있습니다. **차단됐다고 바로 포기하지 마세요.** `WebSearch`는 다른 경로로
나가므로 대개 동작합니다. 아래 질의로 검색하고, **검색 결과 본문에 문자 그대로
나타난 수치만** 사용합니다.

- `KAYAK 후쿠오카 항공권 왕복 평균 요금 10월`
- `부킹닷컴 하카타 호텔 1박 요금 원`
- `부킹닷컴 유후인 료칸 1박 요금 원`

검색 스니펫에 있는 숫자도 **실제 게재된 값이므로 사용해도 됩니다.** 단, 어느 질의의
어느 결과에서 가져왔는지 커밋 메시지에 남기세요. 추론·환산·반올림으로 만들어낸
숫자는 여전히 금지입니다.

`WebFetch`와 `WebSearch` 둘 다 실패한 출처만 "조회 실패"로 처리합니다.

---

## ⚠ 현재 이 루틴은 중단돼 있습니다 (2026-09-03)

클라우드 실행 환경이 `kayak.co.kr`과 `booking.com`을 **네트워크 정책으로 차단**합니다
(`EGRESS_BLOCKED`). `WebSearch` 폴백도 시도했으나 검색 스니펫에는 필요한 개별 수치가
없어, 규칙대로 아무 값도 바꾸지 않았습니다.

즉 **자동 갱신은 현재 불가능**하며, 루틴은 비활성화 상태입니다.

갱신이 필요하면 로컬 Claude Code 세션에서 요청하세요 — 로컬은 egress 제한이 없어
두 사이트 모두 정상적으로 조회됩니다. 나중에 정책이 바뀌거나 접근 가능한 다른 출처를
찾으면 https://claude.ai/code/routines 에서 다시 켜면 됩니다.
